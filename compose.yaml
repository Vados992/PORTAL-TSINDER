services:
  research:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - portal-data:/data
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
volumes:
  portal-data:
